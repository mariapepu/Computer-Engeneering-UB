void bst::inorder{
	inorder_rec(get_root());
}

void bst::inorder_aux(nodetree* node){
	if (node!=nullptr){
		if (node.hasLeft()){
			inorder_aux(node.getLeft());
		}
		cout<<node.getElement();
		if (node.hasRight()){
			inorder_aux(node.getRight());
		}
	}
}